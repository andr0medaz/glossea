<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme utama aplikasi -->
    <style name="Theme.Glossea" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- Theme SplashScreen -->
    <style name="Theme.Glossea.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Glossea</item>
    </style>
</resources>